#ifndef TYPES_H_INCLUDED
#define TYPES_H_INCLUDED

#include <vector>
#include <set>

using TypeId = unsigned long;
using ArchetypeSignature = std::set<TypeId>;
using EntityId = unsigned long;

#endif // TYPES_H_INCLUDED